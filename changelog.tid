title: $:/plugins/rimir/dashboard/changelog

|!Version |!Date |!Changes |
|0.0.32 |2026-07-16 |Fixed: on a board larger than the screen, starting to ''drag or resize'' a tile/group made the canvas ''jump to the top-left'' — the tile flew off to the side (over the wiki menu or off-screen) during the operation and snapped back on release. The drag styling flipped the scrolled canvas to `overflow: visible`, which forces its scroll position to 0. That un-clipping is now scoped to nested group canvases (where it is needed to drag a subgroup out of its parent); the root scroll container keeps its `overflow: auto` and its scroll position throughout the drag |
|0.0.31 |2026-07-16 |Attach files to //any// tile, group or the canvas — not just `work/` tiles — and now by ''clipboard paste'' as well as drop. Hover a tile and a 📋 button appears in its corner: click to //arm// it (a ring shows it's active), then press ''Ctrl+V'' to paste an image from the clipboard. The file is uploaded to the filesystem location ''derived from the target'' via [[rimir/file-upload|$:/plugins/rimir/file-upload]]'s configured scattered locations — a `work/…` target files into the `work` location alongside that item; anything else falls back to `files` — and added as a new tile (a sibling for a content tile, a child for a group/canvas). This generalises the previous `work/`-only drop handling. Also fixed: the 📋 button sits top-left (clear of the ⋮ menu) and is suppressed while a tile's ⋮ menu is open, so menu entries stay clickable and the menu doesn't collapse. Attachments require the file-upload plugin |
|0.0.30 |2026-07-15 |A tile whose target has //no// external link now has an ↗ item ''inside its ⋮ menu'' that opens the target tiddler itself in a new browser tab, via its permalink (`#`-fragment). Complements the corner ↗ (which opens an external `url` / `_canonical_uri`): whichever applies, //open in a new tab// is one click away. Only shown when the target carries none of the configured `external-open-fields` |
|0.0.29 |2026-07-15 |A content tile whose target has an external link now renders its ''caption as a real link'' — hover it to see the address in the browser status bar, click it to open in a new tab. Uses the same configured `external-open-fields` (`url` / `_canonical_uri`) as the ↗ control and Ctrl/Cmd-click; clicking elsewhere on the title bar still opens the target in the wiki and dragging is unaffected |
|0.0.28 |2026-07-14 |Group tiles can now be ''linked to a tiddler'' as their //implicit context// — open a group's ⋮ menu → //Context…// (an editor like //Icon…//), or drop a tiddler onto the group's header. The group adopts the tiddler's caption (editable independently afterwards), shows its [[rimir/kind|$:/plugins/rimir/kind]] icon in the header (click to open the tiddler in the story river), and — when the context is a kind instance — gains the same ✎ //entity// actions a content tile has, with //Add child// creating instances with `parent` pre-set to the context //and dropping their tiles inside this group//. Also fixed: a group's own ⋮ / ✎ menus no longer render //behind// the tiles inside the group |
|0.0.27 |2026-07-14 |A tile whose target has an external link now shows its ↗ //open in new tab// control ''directly in the corner cluster'', next to the ⋮ menu (and the ✎ //entity// control, if any), instead of hidden inside the ⋮ dropdown — so the link is one click away. Only shown when the target carries one of the configured `external-open-fields`; Ctrl/Cmd-click on the tile still does the same |
|0.0.26 |2026-07-14 |Creating a child from a kind-instance tile's ✎ //Add child// now also drops a tile for the new instance onto the dashboard, in the same group as the source tile — so it appears right away. Wired through kind's new post-create hook (a `$:/tags/Macro` global procedure invoked inside `kind.finalize-create`, with the fresh new-instance title in scope). Also fixed menu/entity popups clipping at a group's edge (the nested group canvas is now un-clipped on menu hover, not just the group body). Requires kind ≥ 0.1.85 |
|0.0.25 |2026-07-14 |Tiles whose target is a [[rimir/kind|$:/plugins/rimir/kind]] instance gain a ✎ //entity// control next to the ⋮ menu: hovering it opens a compact popup with ''edit'' (the same live editor modal as //Kind: Instances Overview//) and ''Add child'' buttons for every kind whose `parent` accepts this entity's type (child created with `parent` pre-set, same validation/title-formula as the Creator). Reuses the kind graph-modal via the shared `entity-actions`; only present when kind is installed and the target is an instance. Tile menus/popups no longer clip at a group's edge. Requires kind ≥ 0.1.84 for the entity features |
|0.0.24 |2026-07-14 |''Fullscreen'' now uses the full height: the canvas fills the space beneath the toolbars and scrolls inside itself, instead of a fixed height that left empty space below yet still overflowed the screen with a stray vertical scrollbar. When //Fit to screen// is also on, the fit widget keeps sizing the board as before |
|0.0.23 |2026-07-14 |Added ''Fit to screen'' — a per-dashboard toolbar toggle that scales the whole board down so everything is visible at once, sized to fit your window (shrink-only; a board that already fits stays full-size; the setting is remembered per dashboard). Dragging and resizing stay accurate while fitted. Added a ''Fullscreen'' button that shows just the dashboard on the whole screen (no wiki chrome) via the browser Fullscreen API — press again or Esc to exit; combine the two to see an entire board at once on a small display. Also fixed: a tile's ⋮ actions menu no longer opens //behind// an overlapping neighbouring tile; and a group nested inside another group can now be dragged out to the root canvas or a sibling — previously it was clipped at its parent's edge and could only be dropped into subgroups |
|0.0.22 |2026-07-13 |A ''collapsed'' tile or group is now a drop target — no need to expand it first. Drop a tiddler or file onto a collapsed //group// to add it as a child (the group then expands); drop a file onto a collapsed //content tile// whose `target` is under `work/` to file it alongside that item. No permanent drop band — the box is simply outlined while a drag hovers over it |
|0.0.21 |2026-07-10 |A collapsed tile's (or group's) icon can now be ''resized individually'': hover it and drag the corner grip. The size is stored per tile (`icon-w` / `icon-h`), so each collapsed icon can differ; the settings box size becomes the //default// for icons not yet resized |
|0.0.20 |2026-07-09 |Window content tiles can now be collapsed (▸/▾ in the title bar), like groups: a collapsed tile shrinks to just its headline — or, if its `target` has an `icon`, to just that icon (click to expand, drag to move, hover for the caption). The collapse toggle is now shared between tiles and groups |
|0.0.19 |2026-07-09 |Fixed dragging a collapsed group whose header is an image icon: the icon is now a move + click-to-expand surface, so you can ''drag the image itself'' to move the group (a plain click still expands it). Native image-dragging is suppressed on tile/group icons, so dragging one no longer drops a stray new tile onto the canvas |
|0.0.18 |2026-07-09 |A collapsed group's icon is now scaled to a reasonable box (default 64×64 px) preserving its aspect ratio, instead of being forced into a tiny square — so wide/large image icons render properly. The max width and height are configurable in the plugin settings |
|0.0.17 |2026-07-09 |Image (and PDF) tiles now show the ''thumbnail'' by default when the target has a `_thumbnail_uri`, rendered as an `<img>` scaled to the tile — instead of the full-size original. The same applies when an `icon` (a group's, or a tile's) points at an uploaded image: its thumbnail is used, not the full image. Choose //body// from the ⋮ menu (or a display rule) to force the full image |
|0.0.16 |2026-07-09 |The group //Icon…// action now opens a proper icon editor — common-icon quick-picks, a full-width input (tiddler title or inline markup), a live preview and Clear — rendered at the canvas root. Replaces the cramped in-menu field that was too small and lost focus while typing |
|0.0.15 |2026-07-09 |Group tiles can carry an `icon` (their `icon` field), set/cleared from the group's ⋮ menu. An open group shows the icon small before its name; a collapsed group that has an icon shows //only// the icon — hover for the name, click it to expand |
|0.0.14 |2026-07-09 |Tiles can now show a chosen //field// instead of the body. By default, a tiddler with no text but an `icon` shows its icon. Pick per tile from the ⋮ menu (auto / body / icon / any field), or set per-dashboard ''display rules'' (a target filter → the field to show) via the new //Display rules// panel |
|0.0.13 |2026-07-09 |Tile actions now live behind a ⋮ menu that opens on hover/focus. New ↗ action, and ''Ctrl/Cmd-click'' opens a tile's external link in a new browser tab; the source fields (default `url` then `_canonical_uri`) and their priority are configurable in settings. Plain click still opens the target in the wiki |
|0.0.12 |2026-07-09 |Drop an image on the canvas — or click an empty spot and paste (Ctrl+V) — to upload it via `rimir/file-upload` and add a tile (thumbnail via `rimir/file-pipeline`). Dropping/pasting on a content tile whose `target` is under `work/` stores the file in the `work` location alongside that item. Degrades to drop-only when file-upload is absent. Needs file-upload ≥ 0.1.31 |
|0.0.11 |2026-07-08 |Window- and group-tile caption-bar controls now appear only while the tile is hovered (like the bare-tile controls) and float over the caption, so the title gets the full width and is no longer shortened to make room for the buttons |
|0.0.10 |2026-07-08 |Added a ''Clean'' button that removes tiles from the dashboard after a confirmation showing the exact count. An optional filter box scopes it: empty removes every tile (content //and// groups); a filter removes only tiles whose target it selects |
|0.0.9 |2026-07-08 |Expanding a group now brings it to the front (highest `z`), so its contents are no longer hidden behind other tiles. Collapsing leaves the stacking order alone |
|0.0.8 |2026-07-08 |Added a ''filter'' box above the canvas: type any filter and press //Add matching// to add a tile for every tiddler it selects, laid out in a grid. The filter is interpreted with `subfilter`, so anything valid in a `<$list>` works |
|0.0.7 |2026-07-08 |A collapsed group now shrinks to fit its header (auto width //and// height) instead of keeping its full width; expanding restores the previous width and height (both are preserved while collapsed) |
|0.0.6 |2026-07-08 |Hold ''Ctrl'' (or ''Cmd'') while starting to drag a content tile to ''duplicate'' it: a new tile pointing at the same target tiddler is dropped where you release (into whichever container), and the original stays put. The same tiddler can now appear any number of times across a dashboard |
|0.0.5 |2026-07-08 |Fixed two group-tile bugs: //Add group// now uses the text in the add box as the new group's name (and clears the box); and collapsing a group no longer leaves a stray `yes` where its body was (an empty `<%if%>` then-branch was rendering its bound condition variable) |
|0.0.4 |2026-07-08 |Added expandable/collapsible ''group'' (meta) tiles: a group is itself a free-form canvas of child tiles, and groups nest recursively into each other. //Add group// creates one; the ＋ control adds a nested group; the ▸/▾ control collapses/expands. Tiles (and whole groups) can be dragged between containers — including in and out of the root canvas — to re-home them; the drop target is hit-tested on release. Membership is tracked by a plain `dash-parent` field; group tiles carry `role: group` and an editable `caption`. Removing a group promotes its contents to the parent container |
|0.0.3 |2026-07-08 |Added a ⧉ //show contents// control to each tile that opens the target tiddler in a centered popup, rendered through TiddlyWiki's default ViewTemplate (full title, tags, fields and body). Closes on backdrop click or the × button; popup state is a transient `$:/state` tiddler |
|0.0.2 |2026-07-08 |Kind-aware tiles: a window tile whose target is a `rimir/kind` instance shows the type's `kind.icon` (with the type name as hover text) before the caption; degrades silently without kind. Refreshed the readme to cover z-order, tile styles and click-to-open |
|0.0.1 |2026-07-08 |Initial release: free-form dashboard canvas via `$:/tags/rimir/dashboard`; drop-to-add and //Add tile//; drag-to-move and drag-to-resize (commit on release); persistent `z`-order with bring-to-front; per-tile system-tiddler storage under `$:/config/rimir/dashboard/<id>/tiles/`; //window// and //bare// tile styles with live toggle; click-to-open target in the story river; doc-template documentation |
